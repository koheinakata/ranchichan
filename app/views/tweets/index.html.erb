<!-- 今日のツイート -->
<div class="contents row">
  <% @todaytweets.each do |tweet| %>
    <%= render partial: "tweet", locals: { tweet: tweet } %>
    <%= paginate(@todaytweets) %>
  <% end %>

</div>

<hr>
<div class="changeday">昨日までのランチ</div>

<!-- 過去のツイート -->
<div class="contents row opa">
  <% @tweets.each do |tweet| %>
    <%= render partial: "tweet", locals: { tweet: tweet } %>
  <% end %>
  <%= paginate(@tweets) %>
</div>


